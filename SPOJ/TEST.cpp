#include <bits/stdc++.h>
using namespace std;

int main() {
	   
	ios_base::sync_with_stdio(false);
    cin.tie(0);
	int x;
	while(cin >> x)
		if(x==42)
			break;
		else 
			cout << x << "\n";

	return 0;
}
