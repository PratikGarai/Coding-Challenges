class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        
        mx = nums[0]
        sm = max(nums[0], 0)
        for i in nums[1:] :
            sm += i
            mx = max(mx, sm)
            sm = max(sm, 0)
        
        return mx
